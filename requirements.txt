# YouTube Downloader 의존성
#
# yt-dlp는 YouTube의 변경에 맞춰 수시로 패치되므로 상한을 두지 않는다.
# 오래된 yt-dlp는 "Signature extraction failed" / "Requested format is not available"
# 로 조용히 실패하므로, 문제가 생기면 가장 먼저 아래를 실행할 것:
#   pip install -U yt-dlp
yt-dlp>=2026.7.4

# yt-dlp 선택 의존성 (없으면 일부 사이트·포맷에서 실패)
Brotli>=1.2.0
mutagen>=1.48.1
pycryptodomex>=3.23.0
websockets>=17.0.1

# GUI
PyQt5>=5.15.11

# 댓글 다운로더 (src/youtube_comments.py)
youtube-comment-downloader>=0.1.83

# 네트워크
certifi>=2026.7.22
requests>=2.34.2
urllib3>=2.7.0

# 빌드 도구 (exe / .app 생성 시에만 필요)
pyinstaller>=6.22.1
Pillow>=12.3.0
